import 'package:flutter/material.dart';
import 'colors.dart';

class TextStyles {
  // Buttons
  static const primaryBtnLabel = TextStyle(
    color: Colors.white,
    fontSize: 15,
  );
  static const secondaryBtnLabel = TextStyle(
    color: Colors.white,
    fontSize: 15,
  );
  static const normalBtnLabel = TextStyle(
    color: MainColors.dullBlackOpaque,
    fontSize: 15,
  );
  static const negativeBtnLabel = TextStyle(
    color: MainColors.red,
    fontSize: 15,
  );
}
